"""
version.py — single source of truth for the app's version.

Bump this and tag/publish a matching GitHub Release (see updater.py and the
README's "Releasing an update" section) whenever you cut a new build —
that's what auto-update compares against to decide whether a newer version
exists.
"""

APP_VERSION = "1.0.2"
