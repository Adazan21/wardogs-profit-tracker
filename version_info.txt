# Windows file/product version metadata embedded in profitdog.exe (PyInstaller's
# `version=` param on EXE() in profitdog.spec). Required by SignPath Foundation's
# code-signing terms ("set metadata attributes — product name and version — on
# all signed binaries"), and generally good practice for a distributed exe
# regardless (shows up in Explorer's file Properties > Details tab).
#
# Keep FileVersion/ProductVersion in sync with version.py's APP_VERSION by hand
# when bumping — PyInstaller's version-info format doesn't support importing
# a value at parse time the way the rest of profitdog.spec does.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 3, 0),
    prodvers=(1, 0, 3, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'Addison Wightman'),
            StringStruct(u'FileDescription', u'Wardogs Profit Tracker'),
            StringStruct(u'FileVersion', u'1.0.3.0'),
            StringStruct(u'InternalName', u'profitdog'),
            StringStruct(u'LegalCopyright', u'GPL-3.0 License'),
            StringStruct(u'OriginalFilename', u'profitdog.exe'),
            StringStruct(u'ProductName', u'Wardogs Profit Tracker'),
            StringStruct(u'ProductVersion', u'1.0.3.0'),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])]),
  ],
)
